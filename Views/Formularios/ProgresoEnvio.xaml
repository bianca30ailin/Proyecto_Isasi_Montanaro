<Window x:Class="Proyecto_Isasi_Montanaro.Views.Formularios.ProgresoEnvio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Proyecto_Isasi_Montanaro.Views.Formularios"
        mc:Ignorable="d"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Producto" 
        Height="250" Width="380">

    <!-- Borde redondeado -->
    <Border CornerRadius="8"
        Background="White"
        BorderBrush="#E5E7EB"
        BorderThickness="1">

        <Grid Margin="20,10,20,20">
            <Grid.RowDefinitions>
                <!-- Título -->
                <RowDefinition Height="Auto"/>
                <!-- Borde -->
                <RowDefinition Height="Auto"/>
                <!-- Opciones -->
                <RowDefinition Height="Auto"/>
                <!-- Botones -->
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <StackPanel Grid.Row="0" Margin="0,0,0,10" Style="{StaticResource HeaderWithIcon}">
                <Image Source="/Assets/otros/material-symbols--change-circle-outline.png" Style="{StaticResource FormHeaderIcon}"/>
                <TextBlock Text="Cambio de estado" Style="{StaticResource FormHeaderTitle}"/>
            </StackPanel>

            <Border Grid.Row="1" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Margin="0,0,0,0"/>

            <!-- Formulario -->
            <Grid Grid.Row="2" Margin="20,20,20,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!-- Sexo -->
                <StackPanel Orientation="Horizontal"  
                    Grid.Row="0" Grid.Column="1"
                    HorizontalAlignment="Left"      
                    VerticalAlignment="Center">
                    <RadioButton Content="En preparacion" 
                         GroupName="Sexo"
                         Margin="0,10,40,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0" 
                    Grid.Row="1" Grid.Column="1"
                    HorizontalAlignment="Left"      
                    VerticalAlignment="Center">
                    <RadioButton Content="En camino" 
                         GroupName="Sexo"
                         Margin="0,10,40,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0" 
                    Grid.Row="2" Grid.Column="1"
                    HorizontalAlignment="Left"      
                    VerticalAlignment="Center">
                    <RadioButton Content="Entregado" 
                         GroupName="Sexo"
                         Margin="0,10,40,0"/>
                </StackPanel>
            </Grid>
            <!-- Botones -->
            <StackPanel Orientation="Horizontal" 
                Grid.Row="3" 
                HorizontalAlignment="Center">

                <!-- Cancelar -->
                <Button Style="{StaticResource BtnSecondary}" 
                    Content="Cancelar" 
                    Margin="0,0,10,0"
                    Foreground="White"
                    Background="#757575"
                    Click="Cancelar_Click" Cursor="Hand"/>

                <!-- Modificar -->
                <Button Style="{StaticResource BtnPrimary}" 
                    Content="Modificar"
                    Margin="0,0,10,0"
                    Background="#F5A623"/>

                <!-- Guardar -->
                <Button Style="{StaticResource BtnPrimary}" 
                    Content="Guardar" 
                    Background="#53BA83"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
